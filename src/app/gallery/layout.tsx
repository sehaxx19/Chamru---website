export const metadata = {
  title: "Gallery",
  description:
    "Photos from the road — Sri Lanka's culture, wildlife, beaches, and hiking trails, captured across tours with Travel with Chamru.",
};

export default function GalleryLayout({ children }: { children: React.ReactNode }) {
  return children;
}
