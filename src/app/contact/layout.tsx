export const metadata = {
  title: "Contact",
  description:
    "Get in touch with Travel with Chamru — WhatsApp, call, email, or send a message. I'll reply within 48 hours.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
