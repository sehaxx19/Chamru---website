export const metadata = {
  title: "Build Your Itinerary",
  description:
    "Tell us your dates, interests, and budget — get a suggested Sri Lanka itinerary with driving times and estimated cost.",
};

export default function ItinerariesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
